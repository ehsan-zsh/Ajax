// ما می توانیم به واسطه متد get و post دیتا یا داده های ورودی یک فرم را دریافت کنیم و نمایش بدهیم
// همچنین برای دسترسی به مقادیر ورودی فرم می بایست از name استفاده کنیم

if(isset($_POST) and !empty($_POST)){
	echo $_POST['name'];
}